# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Dart

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - name: Install dependencies
        run: flutter pub get

      # Creating .env file for use by GitHub Actions pipeline
      - name: Create env file
        run: | 
          touch .env
          echo "HEROKU_API=fakeApi" >> .env
          echo "HEROKU_API_KEY=fakeKey" >> .env
          echo "ANDROID_GOOGLE_MAPS_SDK_API_KEY=fakeKey" >> .env
          echo "ANDROID_GOOGLE_MAPS_DIRECTIONS_API_KEY=fakeKey" >> .env
          echo "IOS_GOOGLE_MAPS_SDK_API_KEY=fakeKey" >> .env
          echo "IOS_GOOGLE_MAPS_DIRECTIONS_API_KEY=fakeKey" >> .env
          cat .env

      # Uncomment this step to verify the use of 'dart format' on each commit.
      # - name: Verify formatting
      #   run: dart format --output=none --set-exit-if-changed .

      # Consider passing '--fatal-infos' for slightly stricter analysis.
      - name: Analyze project source
        run: flutter analyze

      - name: Run tests
        run: flutter test
